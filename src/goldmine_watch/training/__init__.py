"""Training loop, validation, checkpointing, and manifest generation.

Modules:
    train: Main training loop with early stopping.
    evaluate: Test-set evaluation and metric computation.
    cli: Command-line entrypoints for training and evaluation.
"""
